# This directory is reserved for service-layer modules.
# Services encapsulate business logic that may be shared across controllers.
#
# Examples of future services:
#   - blockchainService.js  (Stylus Smart Contract interactions)
#   - emailService.js       (transactional emails)
#   - paymentService.js     (payment gateway integration)
